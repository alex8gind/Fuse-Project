import React, { Component } from 'react'
import { connect } from 'react-redux'

export class EmailVarificConfirm extends Component {
  render() {
    return (
      <div>EmailVarificConfirm</div>
    )
  }
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(EmailVarificConfirm)